meta {
  name: gettAll
  type: http
  seq: 1
}

post {
  url: {{api_url}}proFormaInvoices/getAll/?access_token={{access_token}}&{{enable_debug}}
  body: formUrlEncoded
  auth: none
}

params:query {
  access_token: {{access_token}}
  {{enable_debug}}: 
}

body:form-urlencoded {
  company_id: 
}
